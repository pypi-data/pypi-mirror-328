[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flowhive"
version = "0.0.1"
description = "Placeholder to reserve the flowhive name on PyPI."
readme = "README.md"
authors = [
  { name = "Jakob Kristensen", email = "jakob@jakobk.dk" }
]
license = { text = "MIT" }

requires-python = ">=3.7"

