[build-system]
requires = ["git-versiointi>=1.7b1", "setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "django-yleisavain"
dynamic = ["version"]
requires-python = ">= 3.8"
description = "Työkaluja yleisiin vierasavaimiin liittyvien kyselyjen optimointiin"
readme = "README.md"
authors = [
  {name = "Antti Hautaniemi", email = "antti.hautaniemi@pispalanit.fi"},
]
dependencies = ["Django>=4.2"]
urls = {Repository = "https://github.com/an7oine/django-yleisavain.git"}
