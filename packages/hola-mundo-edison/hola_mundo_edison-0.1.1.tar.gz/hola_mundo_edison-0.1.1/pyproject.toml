[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hola_mundo_edison"
version = "0.1.1"
authors = [
    { name = "Edison", email = "ERAMREG@proteccion.com.co" }
]
description = "Una librería simple que dice ¡Hola, mundo!"
readme = "README.md"
license = { file = "LICENSE" }
dependencies = ["numpy"]
requires-python = ">=3.7"
