[project]
name = "andtopic"
version = "0.0.1"
description = "A placeholder package for andtopic brand."
readme = "README.md"
authors = [
  { name = "Shoichi Seto", email = "seto@andtopic.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
