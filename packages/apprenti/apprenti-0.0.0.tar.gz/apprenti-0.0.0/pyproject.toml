[project]
name = "apprenti"
version = "0.0.0"
description = "Coming soon~"
authors = [
    {name = "Abdul Saleh"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "apprenti", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
