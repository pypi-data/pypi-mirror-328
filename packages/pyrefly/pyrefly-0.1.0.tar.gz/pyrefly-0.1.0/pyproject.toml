[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyrefly"
version = "0.1.0"
description = "A type checker for Python written in Rust."
authors = [{ name = "Aaron Pollack", email = "pollackaaron@gmail.com" }]
readme = "README.md"
license = { file = "LICENSE" }
dependencies = []

[project.scripts]
pyrefly = "pyrefly:main"