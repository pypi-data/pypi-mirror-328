[project]
name = "menu_cpk"
version = "0.5.0"
description = "menu package"
authors = [
    {name = "Charles Knell",email = "charles1283@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "menu_cpk", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
