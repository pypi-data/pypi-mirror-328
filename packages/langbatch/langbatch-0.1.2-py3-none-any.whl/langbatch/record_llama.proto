syntax = "proto2";
package record_llama;

// message type representing the rows in your input dataset table.
message BatchRecord {
  required string custom_id = 1;
  required string method = 2;
  required string url = 3;
  required string body = 4;
}