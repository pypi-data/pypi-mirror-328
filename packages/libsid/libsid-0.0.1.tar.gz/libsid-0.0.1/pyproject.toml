[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "libsid"
version = "0.0.1"
description = "Tools for making life easy in 3D"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Siddhant Bansal", email = "siddhant2697@gmail.com"}
]
dependencies = [
    "numpy>=1.26.4",
    "matplotlib>=3.8.4",
    "open3d>=0.18.0",
    "pytorch3d>=0.7.8",
    "torch>=2.6.0",
]

[project.optional-dependencies]
dev = [
]
