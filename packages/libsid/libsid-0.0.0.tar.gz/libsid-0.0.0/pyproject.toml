[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "libsid"
version = "0.0.0"
description = "Tools for making life easy in 3D"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
    {name = "Siddhant Bansal", email = "siddhant.bansal@bristol.ac.uk"}
]
dependencies = [
]
