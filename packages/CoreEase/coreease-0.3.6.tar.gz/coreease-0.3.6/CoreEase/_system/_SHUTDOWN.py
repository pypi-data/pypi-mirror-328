#=========================================================================
def SYS_SHUTDOWN(time:int):
    import os
    os.system(f'shutdown /s /f /t {time}')
#=========================================================================