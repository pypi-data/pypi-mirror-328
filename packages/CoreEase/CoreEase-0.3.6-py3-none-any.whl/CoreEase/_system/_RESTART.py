#=========================================================================
def SYS_RESTART(time:int):
    import os
    os.system(f'shutdown /r /f /t {time}')
#=========================================================================