[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-mri"
version = "0.1.6"
description = "Simple MRI-utility wrapper around nibabel."
readme = "README.md"
authors = [
    { name = "Jørgen Riseth", email = "jnriseth@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "nibabel>=5.0.0",
    "numpy>=1.0.0",
]

[tool.setuptools]
package-dir = {'' = 'src'}
packages = ["simple_mri"]
