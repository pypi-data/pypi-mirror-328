[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cortosis"
license = {text = "Apache 2.0"}
version = "0.0.0"
authors = [{name = "Interstellio IO (PTY) LTD", email = "opensource@interstellio.io"}]
description = "Simple API / WEB and Minion / Daemon frameword"
readme = "README.rst"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Homepage = "https://github.com/interstellio/cortosis"
Documentation = "https://cortosis.readthedocs.io"
