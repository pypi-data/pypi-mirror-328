[project]
name = "lrcfetcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mutagen>=1.47.0",
    "requests>=2.32.3",
    "tqdm>=4.67.1",
]

[project.scripts]
lrcfetcher = "lrcfetcher.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
