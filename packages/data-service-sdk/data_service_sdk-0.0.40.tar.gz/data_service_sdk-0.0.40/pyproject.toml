[build-system]
requires = ["hatchling>=1.8.0", "hatch-fancy-pypi-readme"]
build-backend = "hatchling.build"

[project]
name = "data-service-sdk"
dynamic = ["version", "readme"]
description = "data-service-sdk python sdk"
license = {text = "Apache License 2.0"}
authors = [
  {name = "SAIS Contributors"},
  {email = "sais@example.com"}
]
keywords = ["sais", "auto train data service"]
classifiers = [
    "Programming Language :: Python :: 3.9",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
requires-python = ">=3.9"

dependencies = [
"pydantic~=2.7.4",
"requests~=2.32.3",
"setuptools~=69.5.1",
"pymongo",
"pandas",
"loguru"
]

[project.optional-dependencies]
test = []

[project.urls]
repository = "https://github.com/xxx/xxxx"

[project.scripts]

[tool.hatch.metadata.hooks.fancy-pypi-readme]
content-type = "text/markdown"
fragments = [
  { path = "README.md" },
]

[tool.hatch.version]
path = 'sais/autotrain/dataservice/config/version.py'

[tool.hatch.build.targets.sdist]
exclude = []

[tool.hatch.build.targets.wheel]
packages = ["sais"]

[tool.black]
line-length = 99
target-version = ['py38']
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.venv
  | venv
  | _build
  | buck-out
  | build
  | dist
  | docs
)/
'''

[tool.isort]
profile = 'black'
src_paths = ['sais']
line_length = 99
lines_after_imports = 2
force_single_line = 'True'
force_sort_within_sections = 'True'
order_by_type = 'False'